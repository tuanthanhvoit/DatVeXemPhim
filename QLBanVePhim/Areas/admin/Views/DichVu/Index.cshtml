@model IEnumerable<QLBanVePhim.Models.DichVu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDichVu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaTien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDichVu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaTien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DichVuId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DichVuId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DichVuId })
        </td>
    </tr>
}

</table>
