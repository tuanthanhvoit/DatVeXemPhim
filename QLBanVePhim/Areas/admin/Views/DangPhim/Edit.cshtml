@model QLBanVePhim.Models.DangPhim

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DangPhim</legend>

        @Html.HiddenFor(model => model.DangPhimId)

        <div class="editor-label">
            @Html.LabelFor(model => model.TenDang)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TenDang)
            @Html.ValidationMessageFor(model => model.TenDang)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
