@model IEnumerable<QLBanVePhim.Models.LichChieu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhatHanhPhim.PhatHanhPhimId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayChieu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhatHanhPhim.PhatHanhPhimId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayChieu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LichChieuId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LichChieuId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LichChieuId })
        </td>
    </tr>
}

</table>
